import React from 'react';
import Chatbot from '../components/Chatbot';

// Standalone chatbot page - the chatbot now takes up the full page
const ChatbotPage = () => {
  return <Chatbot />;
};

export default ChatbotPage;
